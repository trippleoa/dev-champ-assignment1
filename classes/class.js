// Classes let us define a blueprint for creating objects with
// properties (data) and methods (actions). 

class Person {
// constructor runs when we create (instantiate) a new Person
constructor(name, age) {
this.name = name; 
this.age = age; 
}
}